// JavaScript function to go back to the previous page
function goBack() {
window.history.back();
}

// Optionally, use JavaScript to dynamically set the employee's name and images
document.getElementById("employee-name").textContent = "Jane Smith";  // Set the employee's name dynamically
